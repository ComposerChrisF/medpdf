# TODO — medpdf workspace

## Current state (2026-07-16)

A deep bug hunt (2026-07-16) filed **37 verified bug reports**: `bugs/bug-0002` … `bugs/bug-0038`.  Every report is self-contained: description, test-ready reproduction, suggested fix, and why the fix is right.  IDs were assigned alphabetically by slug (per the portfolio convention); **priority lives in this file and in each report’s Severity line, not in the ID**.

**Read this file top to bottom — the ordering below is the work plan.**  Reports marked **[NEEDS CHRIS]** contain a spec-level decision; do NOT “just fix the code” for those — the report says exactly what is undecided and why.

## Step 0 — housekeeping (first commit, needs Chris’s go-ahead to commit)

- [x] Commit the 37 new bug reports (bug reports are committed when created, per the portfolio bug-reports rule).  _Done: commit `87f7bbf`._
- [x] In the same or a following commit: **delete `bugs/bug-0001-report-overlay-content-stream-stale-length.md`** — that bug was fixed in v0.10.2 (commit `5c25a85`, pinned by `tests/overlay_length_regression_tests.rs` and `tests/no_raw_stream_content_assignment.rs`); the report was renamed into `bugs/` by `f1a41c0` instead of being deleted.  Commit message should name bug-0001.

## Step 1 — decisions Chris must make (blocks the marked bugs only)

- [ ] **bug-0021 [NEEDS CHRIS]** — `parse_page_spec` silently filters/clamps out-of-range pages.  Document the contract (recommended, non-breaking) or change to error (breaking for pdf-maker/pdf-orchestrator)?
- [ ] **bug-0024 [NEEDS CHRIS]** — `place_page` (x, y) semantics for non-zero-origin MediaBox: compensate so the visible box lands at (x, y) (matches the feature plan; changes behavior + one pinned test), or keep user-space mapping and fix the three docs?
- [ ] **bug-0023 [NEEDS CHRIS]** — `place_page` and source `/Rotate`: honor it in the transform (recommended for imposition) or document it as caller responsibility?
- [ ] **bug-0013 [NEEDS CHRIS]** — image rotation pivot: README says anchor point, code rotates about box center.  Fix the README (recommended; pdf-maker ships on center) or change the code?

## Step 2 — doc-only and test-only fixes (no rulings needed; one session)

- [ ] **bug-0009** — consolidated docs drift: both failing Quick Starts, private KEY constants, stale versions (0.11.0 / 0.4.3), medpdf-image README rewrite, error-table row, CLAUDE.md modules, feature-plan status banners, AES-128 rustdoc, `EmbeddedFontCache` scope note.
- [ ] **bug-0002** — AES-256-named tests actually test AES-128; make the algorithm explicit.

## Step 3 — medpdf core fixes, in dependency order

Order matters here; later fixes reuse mechanisms and helpers from earlier ones.

1. [x] **bug-0007** (Critical, crash) — deep-copy cycle → stack-overflow abort.  Fix: reserve the dest ID in `copied_objects` **before** recursing.  Independent of everything else; do first.  _Done: `pdf_helpers.rs` two-phase copy + `tests/copy_page_cycle_regression.rs` (child-process, pins the fix)._
2. [x] **bug-0018** (High) — stop decode→re-encode of destination content in overlay/place; wrap with standalone `q`/`Q` streams instead; loud error (or correct emitter) for inline images in **source** content; add `LOPDF_INLINE_IMAGE_BUG.md` upstream-defect record at the repo root.  This builds the isolation mechanism that 3 and 6 reuse.  _Done: `isolate_dest_content_streams` (standalone wrappers, no re-encode) + `rename_source_content_streams` (rejects inline images loudly, operator-aware renaming); shared `count_q_balance` promoted to `pdf_helpers`; `LOPDF_INLINE_IMAGE_BUG.md` added; `tests/overlay_inline_image_regression.rs` (4 tests, corruption-detecting ones verified to fail on revert) + operator-aware unit tests._
3. [x] **bug-0019** (Medium-High) — source fragments must be decoded as one concatenated stream (dest side is already fixed by 0018).  _Done: `rename_source_content_streams` now concatenates all source fragments (newline-joined), decodes once, re-emits a single combined stream; `tests/overlay_split_operation_regression.rs` (overlay + place, verified to fail on per-fragment revert)._
4. [x] **bug-0030** (High) — normalize `Reference`-valued resource sub-dicts after deep copy; deref in `add_resource_keys` and in the merge’s dest side.  _Done: `normalize_resource_subdicts` (inline indirect source sub-dicts) + `add_resource_keys` derefs references for the collision scan + merge dest-side derefs a referenced sub-dict; `tests/overlay_indirect_resources_regression.rs` (4 tests, each facet verified to fail on targeted revert).  Deferred the “treat /Kids-without-/Type as a Pages node” related note (separate lenient-input concern, would need its own test)._
5. [x] **bug-0017** (High) — inherited `/Resources` handling in overlay (dest seed + source walk) and place (source walk).  Build one shared `resolve inherited attribute` helper — **bug-0008** consumes it too.  _Done: `pdf_helpers::get_page_resources` (walks `/Parent`, the shared helper bug-0008 reuses); overlay/place resolve the source’s effective resources through it; the merge materializes inherited resources onto a dest page and privatizes their sub-dicts (via `inline_resource_subdicts`, refactored from bug-0030’s normalize) so a shared ancestor sub-dict isn’t polluted; `tests/overlay_inherited_resources_regression.rs` (4 tests — 3 facets + shared-subdict privatization, each verified to fail on targeted revert)._
6. [x] **bug-0025** (Medium-High) — `place_page` destination isolation, using 0018’s wrapper mechanism.  _Done: `place_page` now wraps the destination content with `isolate_dest_content_streams` (the bug-0018 standalone-q/Q primitive, no re-encode) before appending the placement, so a leaked dest CTM can’t displace the placed page; `tests/place_page_isolation_regression.rs` (verified to fail on revert)._
7. [x] **bug-0008** (High) — `copy_page` materializes inherited MediaBox/Resources/CropBox/Rotate (uses the step-5 helper).  _Done: `copy_page_with_cache` walks the source page’s `/Parent` chain (via the generalized `pdf_helpers::resolve_inherited_attribute`, which `get_page_resources` now delegates to) and flattens each inherited attribute the copied page lacks onto the leaf page, deep-copying reference values through the shared map; `tests/copy_page_inherited_attributes_regression.rs` (verified to fail on revert)._
8. [x] **bug-0020** (High) — `/Count` maintenance: ancestor walk on delete; +1 (never `kids.len()`) on copy/blank.  _Done: `pdf_helpers::adjust_ancestor_counts` walks the `/Parent` chain applying ±1 (cyclic-chain guarded); `delete_page` decrements every ancestor, `copy_page`/`create_blank_page` increment via the walk (never `kids.len()`); `delete_page` doc comment corrected; `tests/page_tree_count_regression.rs` (4 nested-tree tests + flat sanity, all four verified to fail under the `MEDPDF_TEMP_BUG0020` pre-fix guard)._
9. [x] **bug-0016** (Low) — overlay from a contentless source page becomes a no-op.  _Done: `overlay_page` matches on `/Contents` and returns `Ok(())` (debug-logged no-op) when absent, mirroring `place_page` (`/Contents` is optional — PDF 32000-1 §7.7.3.3); `tests/overlay_contentless_source_regression.rs` (overlay no-op + place sibling guard, overlay test verified to fail with `Err(DictKey)` on revert)._
10. [x] **bug-0037** (High-when-triggered) — watermark font/GS key collision: collision-check with `find_unique_name`.  _Done: `unique_resource_key` (in `pdf_watermark.rs`) reads the page’s effective `/Font`/`/ExtGState` sub-dict (inherited-aware) and, on a collision with a **different** object, preserves the existing binding and derives `F{id}_w`/`GS{id}_w` via `pdf_overlay_helpers::find_unique_name`; an idempotent same-object re-registration keeps the natural key, and the no-collision case is byte-stable; both `register_font_in_page_resources` and `register_extgstate_in_page_resources` route through it; `tests/watermark_resource_key_collision_regression.rs` (collision-survives + natural-key-preserved, collision test verified to fail on revert)._  **Deferred bloat notes from the bug-0037 report (not corruption — pure bloat; do opportunistically): (a) `add_known_named_font` allocates a new built-in font object per draw call — a per-document base-font-name cache would dedup; (b) `push_alpha_ops` adds a fresh ExtGState per call even for identical alpha; (c) `insert_content_stream` creates its q/Q wrapper streams before discovering the page has no `/Contents`, orphaning two objects in that branch.**

## Step 4 — font subsystem (independent of Step 3; can interleave)

1. [x] **bug-0031** (High) — scale `/Widths` + FontDescriptor metrics to 1000/em glyph space (the composite `/W` code shows the formula).  _Done: `font_helpers::glyph_space_scale` (1000/upem) + `scale_metric`; `get_font_widths`, `get_pdf_font_bbox`, and `get_pdf_info_of_face` now scale every advance and descriptor metric into glyph space (italic_angle/stem_v left unscaled — not font-unit metrics); `FontDescriptorPdfInfo` metric fields widened `i16`→`i32` and `bbox_as_object_array` follows, so a small-upem font can’t overflow when scaled up; `tests/font_metrics_scaling_regression.rs` (font-independent — computes expected from the loaded face; verified to fail under `MEDPDF_TEMP_BUG0031`, emitting Arial’s raw `Widths['A']=1366` vs correct 667).  `valign_test.pdf` regenerated (correct embedded-font metrics shift its bytes)._
2. [x] **bug-0012** (High on macOS) — font-kit `font_index` dropped → collection fonts embed the wrong face with `BaseFont=Unknown`.  _Done via **Plan A** (additive, NO public-API change — the pdf-maker/pdf-orchestrator audit showed neither constructs/matches `FontData`/`FontPath`, and both only pass the index-free value through, so threading the index into the API buys nothing and was dropped).  Two loud-fail guards: `handle_to_font_path` errors on a nonzero face index at resolution; `add_text_params` refuses a `.ttc`/`.otc` collection blob at embed (catches face-0 collections + directly-constructed `FontData::Embedded`).  Verified against `~/Chris/Sibelius` `.pdfOrch` fonts — all bare-family requests (Bell MT Std, Source Sans Pro) are regular + single-face, so none are affected; only genuinely-broken styled-`.ttc` cases now error.  `tests/font_collection_rejected_regression.rs` (deterministic collection-blob refusal + a Helvetica-Bold invariant test; both verified to fail under `MEDPDF_TEMP_BUG0012`, reproducing `BaseFont=["Unknown"]`)._  **Follow-up — Plan B (deferred, not yet filed):** extract the selected face from a collection into a standalone single-face sfnt at resolution time (still no API change) so styled system fonts embed correctly; reuses the allsorts font-writing in `pdf_subset.rs` and interacts with bug-0034 (head checksum) and bug-0005 (CFF/OTF).  Also: legacy `~/Chris/Sibelius/.PdfOrchConfig-Main.xml` still has dormant Windows `C:\` font paths (automake runs only the `#Make2` chain).
3. [x] **bug-0005** (Medium-High) — CFF/OTF: `/Type1` font-dict subtype, FontFile3 `/Subtype /OpenType`, no `Length1`; composite CFF → loud error (or CIDFontType0).  _Done: `font_helpers::classify_font` now returns a `FontClassification` (dict_subtype/font_file_key/stream_subtype/emits_length1/is_cff) — CFF ⇒ Font-dict `/Type1` (was the illegal `/Type1C`), FontFile3 stream `/Subtype /OpenType`, no `/Length1`; TrueType (`glyf`) unchanged (`/TrueType` + FontFile2 + `/Length1`).  `add_descriptor_and_fontfile` emits the stream `/Subtype`/`/Length1` per flavor; the composite (Type0/CIDFontType2) path fails loud on a CFF face (CIDFontType0 not yet implemented — directly improves Bell MT Std, Chris’s CFF font).  `tests/cff_otf_font_structure_regression.rs` (CFF simple structure + composite fail-loud + TrueType-unchanged guard; both CFF tests verified to fail under `MEDPDF_TEMP_BUG0005`; composite test uses `lossy_text` + a neutral font name + a `CIDFontType0`-token assertion to avoid a false pass)._
4. [x] **bug-0032** (Medium) — simple path fails loudly on missing glyphs (mirror the composite arm); plus the lossy-`.notdef` width and control-char notes in the report.  _Done: the WinAnsi simple arm now runs `encode_text_winansi_checked` for embedded fonts — a **printable** char the face lacks fails loud with `UnrepresentableText` (or substitutes `?` + `log::warn!` under `lossy_text`), mirroring the composite arm; built-in fonts (no face) keep today’s behavior.  Lossy `.notdef` skew fixed in `build_w_array`: a `/W` entry for GID 0 is always emitted (real .notdef advance) so a substituted glyph no longer advances DW=1000.  `tests/simple_font_missing_glyph_regression.rs` (fail-loud + lossy-`?` + control-char-not-error; the two fail-loud tests verified to fail under `MEDPDF_TEMP_BUG0032`; the gap is manufactured by subsetting a system font to “AB” via medpdf’s own `subset_fonts`, since all system fonts cover full WinAnsi) + `pdf_font_composite::w_array_always_includes_notdef_gid0`.  **Control-char handling DEFERRED per Chris (2026-07-23):** not rejected — `add_text_params` now `log::warn!`s (naming each control char) that medpdf renders a single line and does not interpret `\n`/`\t`, so a debugging reader finds the cause.  Whether to support multi-line (and where the fix lives) is a joint pdf-maker/medpdf decision: analysis + the “does medpdf need an API change?” answer in `feature-plan-multiline-watermark-text.md` (basic `\n`-split: no; leading/wrap/truncate: yes — new `AddTextParams` fields).  Consumers notified: pdf-maker `bug-0016`, pdf-orchestrator TODO.md “Known medpdf Limitations”.  If we later decide against multi-line, escalate to the report’s option #1 (reject control chars loudly)._
5. [x] **bug-0004** (Medium) — omit `/Encoding` for built-in Symbol/ZapfDingbats.  _Done: `add_known_named_font` no longer writes `/Encoding /WinAnsiEncoding` for the two symbolic Standard-14 fonts (new `is_symbolic_standard_14` guard: `Symbol`/`ZapfDingbats`) — WinAnsiEncoding remapped their byte codes to Latin glyph names they lack, rendering a blank page (PDF Annex D binds WinAnsi to nonsymbolic fonts only); mirrors the embedded path’s `determine_pdf_encoding`.  Nonsymbolic built-ins keep WinAnsiEncoding.  README font section gained a sentence: `@Symbol`/`@ZapfDingbats` text is byte codes in the font’s built-in encoding.  `tests/builtin_symbol_font_encoding_regression.rs` (Symbol + ZapfDingbats omit `/Encoding`, Helvetica keeps it; the two symbolic tests verified to fail under `MEDPDF_TEMP_BUG0004`)._
6. [x] **bug-0010** (Medium) — symbol-font cmap lookups via the 0xF000 convention; fix the Webdings misclassification.  _Done: new `font_helpers::glyph_index_symbol_aware` consults the Microsoft `(3,0)` symbol cmap (glyphs at `0xF000+code`), which `Face::glyph_index` skips (Unicode subtables only).  Routed through it: `get_font_widths` (symbol fonts now get real advances, not all-zero → glyph pile-up), `compute_char_range` symbolic scan, and `detect_is_symbolic`’s `has_some_glyphs` probe (Webdings — name heuristic misses it — is now classified symbolic and loses `/Encoding /WinAnsiEncoding`).  **Also fixed the bug-0032 interaction I’d introduced:** `encode_text_winansi_checked` and both width-measurement paths (`pdf_watermark::measure_text_width_with_face` + public `font_helpers::measure_text_width`) are now symbol-aware, so an embedded symbol font draws (bytes render symbol glyphs, not `UnrepresentableText`) and aligns against real advances.  `tests/embedded_symbol_font_regression.rs` (Wingdings nonzero widths + no `/Encoding`, Webdings symbolic classification, non-lossy draw succeeds, nonzero measured width; all 4 verified to fail under `MEDPDF_TEMP_BUG0010`).  Text fonts unaffected (Unicode tried first; Arial has no `(3,0)` subtable)._
7. [x] **bug-0011** (Low) — fallback width: chars, not bytes.  _Done: `compute_text_metrics`’ non-embedded (built-in) width fallback now counts `text.chars().count()` not `text.len()` (bytes), matching `font_helpers::measure_text_width`’s commented intent — byte-counting over-measured multibyte text by 0.6 em/extra byte, mis-centering it.  `tests/fallback_width_char_count_regression.rs` (“Café” centered → Td x = 85.6 not 82.0; verified to fail under `MEDPDF_TEMP_BUG0011`)._
8. [x] **bug-0038** (Low) — `get_font_widths` u8 overflow → usize arithmetic.  _Done: the width-vec length is now `last as usize - first as usize + 1` (and the index write is widened likewise) — a symbolic font spanning `(0, 255)` made `(255u8 - 0 + 1)` overflow (debug panic / release wrap-to-empty then out-of-bounds write).  Unit test `font_helpers::tests::get_font_widths_full_range_does_not_overflow` (asserts `len == 256`; verified to panic “attempt to add with overflow” under `MEDPDF_TEMP_BUG0038`)._
9. [x] **bug-0034** (Low) — `head` directory checksum computed with adjustment zeroed.  _Done: `rebuild_ttf` now computes the `head` table’s directory checksum via new `head_directory_checksum` (OpenType requires `checkSumAdjustment`, bytes 8..12, treated as zero — it subtracts that aligned word from `ttf_checksum`); it had checksummed the head verbatim with its old nonzero adjustment, leaving the entry wrong by that value (OTS-class sanitizers can reject the font).  Unit test `pdf_subset::tests::rebuild_ttf_head_directory_checksum_zeroes_adjustment` (identity rebuild, recomputes the spec checksum from the output; verified to mismatch under `MEDPDF_TEMP_BUG0034`)._

## Step 5 — place_page semantics (after Step 1 rulings)

- [ ] **bug-0024** per ruling (origin compensation or doc fix) — remember `place_page_tests.rs:375` pins today’s behavior.
- [ ] **bug-0023** per ruling (`/Rotate` in transform or documented caller responsibility).
- [ ] **bug-0027** (Low-Medium) — clip with the transformed quad, not its AABB.
- [ ] **bug-0026** (Low) — validate x/y/rotation finiteness.

## Step 6 — medpdf-image

1. [ ] **bug-0029** (Critical for affected inputs) — skip recompression when `/DecodeParms` has Predictor 2 (or is unresolvable).
2. [ ] **bug-0028** (High) — skip recompression when `/Mask` present.
3. [ ] **bug-0033** (Medium) — per-axis downsample clamping for Stretch.
4. [ ] **bug-0035** (Medium) — transparency `/Group` on the SVG Form XObject.
5. [ ] **bug-0014** (Medium) — document the max-DPI JPEG re-encode; quality parameter defaulting to 85.
6. [ ] **bug-0003** (Medium-Low) — premultiplied-alpha resampling.
7. [ ] **bug-0015** (Low-Medium) — JPEG SOF validation: reject SOF3, non-8-bit precision, zero dimensions.
8. [ ] **bug-0036** (Low-Medium) — SVG decompression failure becomes a loud error.
9. [ ] **bug-0013** per ruling — rotation-pivot doc or code.
10. [ ] **bug-0006 [NEEDS FIXTURE]** — CMYK APP14 `/Decode`: obtain a real Adobe CMYK JPEG and verify before implementing.
11. [ ] **bug-0022** (Low, pdf-test-visual) — drain child pipes concurrently with the timeout wait.

## Standing instructions for whoever picks this up

- Every fix must land with a regression test that **fails when the fix is reverted** (several reports name the exact assertion).  For bug-0007 the repro must run in a child process — the failure is a process abort.
- Fixing a bug: commit any report amendments first, then delete the report (and its `bugs/bug-NNNN/` dir, if any) in the commit that lands the fix, naming the ID (portfolio bug-reports rule).
- Both crates are published on crates.io.  Batch behavior-changing fixes into a coherent release; bug-0012 changes public API (semver).  Audit pdf-maker (and pdf-orchestrator) call sites before changing `parse_page_spec` (0021) or `place_page` semantics (0023/0024).
- Code commits go through `/commit` (the dispatcher picks `commit-rust-workspace`); doc-only commits may be bare, but still push.
